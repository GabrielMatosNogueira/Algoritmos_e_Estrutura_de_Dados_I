#include <stdio.h>

int main(void)
{
    int quantidade_Numeros=0;
    int i=0;
    int soma=0;
    int menor=100000;
    int target=0;
    
    scanf("%d", &quantidade_Numeros);
    scanf("%d", &target);

    int array[quantidade_Numeros];

    for(i=0; i<quantidade_Numeros; i=i+1)
    {
        scanf("%d", &array[i]);
        getchar();
    }
    
    for(i=0; i<quantidade_Numeros; i=i+1)
    {
        if(array[i]<menor)
        {
            
        }
        else
        {

        }
    }




}
